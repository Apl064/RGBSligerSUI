//
//  CustomSliderView.swift
//  RGBSligerSUI
//
//  Created by Pavel Metelin on 17.05.2022.
//

import SwiftUI
/*
struct CustomSliderView: View {
//    @Binding var value: Double
//    @Binding var valueText: String
    let value: Double
    let color: Color
    
    var body: some View {
        HStack(spacing: 8) {
            Text("111")
                .frame(width: 35, height: 30, alignment: .leading)
            Slider(value: value, in: 0...255, step: 1)
                .tint(color)
            CustomTextField(valueTextField: $valueText)
        }.padding(.horizontal, 8)
    }
}

struct CustomSliderView_Previews: PreviewProvider {
    static var previews: some View {
        CustomSliderView(value: <#Double#>, color: .black)
    }
}

struct CustomTextField: View {
    @Binding var valueTextField: String
    
    var body: some View {
        TextField("", text: $valueTextField)
            .frame(width: 35, height: 30, alignment: .center)
            .border(.black, width: 1)
    }
}
*/
